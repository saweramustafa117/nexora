# ⚠️ DEMO ONLY — this key is bundled into the client and visible in browser devtools.
# Use a backend proxy in production. For portfolio demos, set a key with usage limits.
VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional — defaults to openrouter/free (auto-selects a free model)
# VITE_OPENROUTER_MODEL=openrouter/free
